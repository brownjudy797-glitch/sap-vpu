# SAP-VPU Codex Instructions

- 默认使用中文回复。
- 修改代码前先说明准备改哪些文件。
- 不要把第三方核源码直接提交进仓库；通过 `scripts/fetch_corev_cv32e40x.sh` 拉到 `third_party/`。
- 总结性记录按日期合并到 `docs/records/YYYY-MM-DD.md`。
- 日期记录标题必须包含主题，例如 `# SAP-VPU Work Records - 2026-07-01 - Project bootstrap`。
- `docs/PROJECT_DOC_INDEX.md` 只做索引，不要合成巨型全文档。
- 生成目录 `work/`、`reports/`、`results/`、`logs/`、`netlist/` 不加入 Git。

