# SAP-VPU Work Records - 2026-07-05 - TinyViT smoke expansion

- Recorded the TinyViT smoke evidence in `docs/SAP_VPU_TINYVIT_SMOKE_RECORD.md`
  and linked it from the project document index.
- Expanded the TinyViT MLP smoke so each policy tile runs 16 deterministic
  iterations instead of a single tiny tile.
- Added testbench checks for exact TinyViT output, active MAC count, and skip
  count, so the smoke now guards the intended workload size.
- Verified the expanded smoke through `make tinyvit-smoke` and regenerated the
  TinyViT counter summary.
- Added a derived TinyViT compute-shape summary for vector lanes, VDOT ops,
  active products, skipped products, and active products per cycle.
- Corrected TinyViT skip ratio reporting to use issued VDOT lanes and the
  hardware skipped-products counter instead of raw VDOT issue count.
- Added a static INT2 TinyViT smoke row with exact output, counter checks, CSV
  export, and derived compute-shape reporting.
- Moved TinyViT smoke operand/weight packed words into a RAM scratch tile,
  changed kernels to load them with `lw`, and added a testbench assertion for
  480 RAM tile reads.
- Added per-kernel observed TinyViT RAM traffic CSV fields for operand reads,
  weight reads, and total tile reads, then updated the summary and smoke record
  tables to use those observed counts.
- Expanded each TinyViT policy kernel from one RAM-loaded micro-block to two
  micro-blocks per iteration, doubling VDOT count and observed RAM tile reads
  while keeping exact output and traffic assertions.
- Refactored the TinyViT tile into a looped four-block RAM layout with
  `TINYVIT_BLOCKS`, raising dense to 256 VDOT ops and total observed tile reads
  to 1920 while preserving exact checks.
