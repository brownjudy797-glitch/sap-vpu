# SAP-VPU Work Records - 2026-07-01 - Project bootstrap

## Bootstrap

- Created a clean SAP-VPU project folder outside the legacy `nutvpu` repository.
- Kept SAP-VPU naming independent from NutShell.
- Added initial docs, Makefile, CV32E40X fetch helper, and CV-X-IF adapter skeleton.
- GitHub repository creation still depends on available GitHub credentials or manual creation.

## IEEE paper-facing v0

- Added a shared SAP-VPU package, first front-door SAP-VPU core model, and bare-metal custom-0 encoding header.
- Extended the CV-X-IF adapter contract with VPU command IDs and response exception propagation.
- Added Verilator adapter/core smoke tests and an ISA encoding checker.
- Added a paper roadmap and legacy baseline ledger so `nutvpu` evidence remains traceable without vendoring old sources.
- Fetched CV32E40X into ignored `third_party/cv32e40x` at short commit `d952cd6` through `make corev-fetch`.
