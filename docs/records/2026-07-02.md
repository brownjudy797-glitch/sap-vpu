# 2026-07-02 Engineering Record

## Verilator timing toolchain

- Installed Ubuntu packages for `g++-10` and `clang-12` to stop avoiding Verilator timing support.
- Kept the successful path on `clang++-12`; `g++-9` lacks `<coroutine>` and `g++-10` still ICEs on generated CV32E40X code.
- Replaced the temporary C++ hello harness with a pure SystemVerilog timing testbench.
