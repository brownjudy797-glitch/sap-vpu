# SAP-VPU Work Records - 2026-07-25 - Synopsys server and 65 nm library audit

## Objective

Validate the school's newer Synopsys toolchain, identify the installed 65 nm
libraries, and determine whether they can support TSMC paper-facing PPA.

## Environment

- Host: `gl01`, accessed through SSH as `2024student144`.
- Design Compiler: `T-2022.03-SP5-2`.
- PrimeTime: `W-2024.09-SP4-1`.
- VCS: `V-2023.12-SP2`.
- License: `27020@gl01`.
- Diagnostic library: STMicroelectronics CMOS065_LP
  `CORE65LPSVT_nom_1.00V_25C.db`.

The library package provides worst-case, best-case, physical, and Verilog
views. Its documentation explicitly identifies STMicroelectronics, so it is
not a TSMC library and cannot supply paper-facing TSMC PPA. The nominal `.db`
checksum is
`b6c9d7d4d7413a731ad9d6db80b39d63967ad8f77d27cca0de5fe6d3c96d0b3b`.

## Mapping Diagnostic

Current core and subsystem RTL passed full `compile_ultra` mapping. DC generated
DDC, Verilog, SDC, QoR, area, timing, constraint, and power reports without an
`Error` or internal fatal message.

| Design | Corner | Slack ns | Critical path ns | Cell area | Cells |
| --- | --- | ---: | ---: | ---: | ---: |
| Core | nominal 1.00 V, 25 C | +1.712 | 7.92 | 27,589.639 | 5,784 |
| Subsystem | nominal 1.00 V, 25 C | +0.0228 | 8.63 | 45,834.879 | 9,327 |
| Subsystem | worst 0.90 V, 125 C | +0.0003 | 9.50 | 46,870.199 | 9,809 |

| Design/corner | Internal power | Switching power | Leakage power | Total power |
| --- | ---: | ---: | ---: | ---: |
| Core nominal | 0.6108 mW | 0.00649 mW | 0.000869 mW | 0.6181 mW |
| Subsystem nominal | 1.1974 mW | 0.0330 mW | 0.00152 mW | 1.2319 mW |
| Subsystem worst | 1.0097 mW | 0.0264 mW | 0.0162 mW | 1.0523 mW |

The rows are vectorless DC estimates from a non-TSMC diagnostic library. They
must not be used in the paper or compared with the FPGA SAIF matrix.

## Gate Activity and PrimeTime Diagnostic

The mapped subsystem passed a VCS gate simulation using the matching ST cell
model and the existing `sap_vpu_subsystem_gate_tb`. The
`fc2_k512_stream_dense` smoke completed 512 `VDOT` operations, 516 reads, and
4 writes in a 66,095,000 ps activity window.

The resulting SAIF has SHA-256
`aacb3a2c7e4302a401c064565b35bdbd6df3eeda33723a6cfc3ef66a8f67b610`.
PrimeTime reported:

| Check | Result |
| --- | ---: |
| Annotated nets | 10,258 / 10,258 (100%) |
| Fully annotated leaf cells | 9,323 (99.99%) |
| Setup slack | +0.0228 ns |
| Hold slack | +0.0663 ns |
| Internal power | 0.6251 mW |
| Switching power | 0.0994 mW |
| Leakage power | 0.000821 mW |
| Total power | 0.7253 mW |

PrimeTime emitted no Error or Fatal. Of 2,409 warnings, 2,406 are
`PLIB-166/167` messages from the ST library's conditional power tables; the
remaining messages are SDC-version and power-unit notices. This result proves
the reusable gate-activity and PrimeTime flow only. It is not TSMC PPA and
must not enter the paper tables. The ST diagnostic still has 5 maximum
capacitance and 145 maximum transition violations, so it is not a closed
physical implementation.

## Next Evidence

1. Obtain an authorized TSMC standard-cell timing `.db` compatible with the
   school's DC installation.
2. Repeat core and subsystem mapping with explicit TSMC process/corner labels.
3. Run TSMC gate smoke, setup/hold analysis, matched TinyViT SAIF, and
   PrimeTime power.

## Transformer Fixture Diagnostic

The TSMC library permission blocker does not prevent model-mapping work.
`make transformer-fixture-check` now loads the existing TinyViT-5M checkpoint
and the official DeiT-Tiny checkpoint, runs the same local real image through
both, and captures one MLP linear-layer input plus the first output pair.

| Model | Captured layer | K | Layer outputs | Captured tokens |
| --- | --- | ---: | ---: | ---: |
| TinyViT-5M | `stages.1.blocks.0.mlp.fc2` | 512 | 128 | 784 |
| DeiT-Tiny | `blocks.5.mlp.fc1` | 192 | 768 | 197 |

The common K8 tiled-GEMM reference reproduces each fixture's dense output and
both sparse-policy outputs exactly. For the selected output pair, TinyViT's
global/budget policies skip 10.55%/12.50% of K4 weight groups; both DeiT
policies skip 18.75%. The result proves that the current linear and metadata
contract can represent a second Transformer shape.

The K=192 fixture now also passes `make sim-subsystem-deit-stream` for output
pairs `(0,1)`, `(256,257)`, `(510,511)`, and `(766,767)`. Dense execution
completes 96 K8 blocks with 768 VDOTs, 784 reads, and 16 writes. Global-12.5%
passes at `652/750/16` with 62 saved payload reads; L1-budget-5% passes at
`656/752/16` with 60 saved reads. All 16 INT32 outputs, `MAC_ACTIVE`,
`DMA_READ_SAVED`, and total OBI transaction counts are checked.

This remains an unlabeled one-image, two-token, eight-output result rather than
full DeiT accuracy, full-layer coverage, or PPA. Imagenette downloads from the tested S3,
Hugging Face mirror, and GitHub release paths were too slow to complete in the
available command window. The incomplete `.part` files were deleted and are
not evidence artifacts. The next model-facing steps are labeled validation and
broader multi-image DeiT coverage.

## Multi-Image Transformer Reference

`make transformer-multi-image-check` now scans the eight local images under
`work/tinyvit_eval_images`, loads TinyViT-5M and DeiT-Tiny once each, and
captures two linear-layer token rows per image. The generated ignored fixture
has SHA-256
`22654f859a0b9318c1aa9024fd48cf69043b873918d354532722860b4a5fcc10`.

| Model | Images | Captured rows | Unique rows | Layer token samples |
| --- | ---: | ---: | ---: | ---: |
| TinyViT-5M | 8 | 16 | 16 | 6,272 |
| DeiT-Tiny | 8 | 16 | 16 | 1,576 |

For both models, the first representative output pair also produces 16 unique
dense result rows. The common checker reproduces all four representative pairs
for dense, global-L1-12.5%, and L1-budget-5% policies. The original single-image
fixture and all three DeiT `VTSTREAM` RTL policies were rerun afterward and
passed with unchanged counters.

This closes local multi-image host reference coverage only. The images remain
unlabeled, so this is not a classification-accuracy result.

## Second-Image DeiT RTL

The existing parameterized flow was rerun with the local road-scene input
`work/tinyvit_eval_images/hybridnets.jpg`; no image-specific RTL was added. The
image SHA-256 is
`ffdd49c47b6f27a5a2bf5f79eb06d8c1f7ca58ff75496542dfb013b9c13a434c`, and
the ignored fixture SHA-256 is
`d21f411387dd05b18f00a6be2a92f3cb66d921a440486944cdf2e20dc6e9d1af`.

Dense, global-L1-12.5%, and L1-budget-5% subsystem runs passed with the same
weight-dependent counters as the dog-image run: `768/652/656` VDOTs,
`784/750/752` reads, and 16 writes. All 16 checked output values differ between
the images in each of the three policies. This proves that two distinct image
activation sets traverse the K=192 stream datapath. It does not prove labeled
accuracy, simultaneous batching, full-layer output coverage, or full-model
DeiT execution.

## WSL Python Runtime Incident

Repeated WSL `/usr/bin/python3.8` reference checks produced inconsistent
interpreter failures in unchanged code. Kernel `dmesg` also records a separate
`python3.8` `SIGSEGV` from `unattended-upgr`, confirming that this is not a
deterministic fixture-checker failure. Available memory was above 61 GiB.

The Transformer model flow already requires the Windows Python 3.14 environment
that contains PyTorch and timm. `run_transformer_generalization.ps1` now also
handles fixture checking and SVH generation through that same interpreter.
Five consecutive direct checks, the Make targets, and the subsequent DeiT RTL
simulation passed. The WSL Python 3.8 installation remains an environment issue
and should be replaced or repaired before it is used for new long-running Python
experiments.
