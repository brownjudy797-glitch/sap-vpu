# SAP-VPU Work Records - 2026-07-07

- Committed the standalone VPU FPGA timing-closure change as
  `a606c87 perf: pipeline vdot for fpga timing`. The commit keeps the claim
  scoped to `sap_vpu_core` post-route timing, not full-SoC or board validation.
- Added a TinyViT smoke policy row, `adaptive_sparse75`, using INT4 bitmap
  `0x03` and two active lanes to cover a structured 75% product-skip point.
  This reuses the existing deterministic INT4 tile and records output,
  MAC-active, skip, sparse/lane state, cycle/instret, and RAM tile reads in the
  generated CSV.
